import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

it('renders without crashing', () => {});
// The reason we deleted the test is that the backend is not here during testing
// Use a mockup
